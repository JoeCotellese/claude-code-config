<!-- ABOUTME: Definition of Done results for issue #53, the /submit domain gate inversion. -->
<!-- ABOUTME: Records each acceptance criterion, the channel that observed it, and the evidence. -->

# DoD Results — #53 domain gate

- Date: 2026-09-07 09:08 EDT
- Issue: #53 — Fix: /submit domain gate blocks every repo without a dedicated reviewer
- Branch: fix/53-domain-gate
- Commit: ed01f4d
- Status: PASS
- Effort label: effort/S

## Harness

Domain detects as `unknown` (shell and markdown config repo), so the harness was chosen by
the user rather than by the detector, per `/verify` Step 2.

- Unit suite: `scripts/tests/check_dev_jawn.sh`, `check_hook_quiet.sh`, `check_issue_37.sh`
- Acceptance channel: the 13 `DOMAIN` assertions inside `check_dev_jawn.sh`
- Result: 46 PASS, 0 FAIL across all three scripts. Output pristine, no unexpected stderr.

## Criteria

- **AC1 — unknown continues instead of stopping.** [test: DOMAIN unknown does not stop submit] PASS.
  Two assertions: no `unknown.*STOP` in SKILL.md, and an acknowledge-and-continue route present.
- **AC2 — error table carries the collision case, not an unknown stop.** [test: DOMAIN error table has no unknown stop] PASS.
  Two assertions: no table row matching `unknown.*STOP`, one row matching `ambiguous.*STOP`.
- **AC3 — three reviewer routes unchanged.** [test: DOMAIN three reviewer routes intact] PASS.
  `swift-swiftui-reviewer`, `python-code-reviewer`, `cpp-qt-reviewer` all still named in the skill.
- **AC4 — detector reports a collision.** [test: DOMAIN detector reports collision] PASS.
  Behavioral: a temp dir holding both `Package.swift` and `pyproject.toml` returns
  `ambiguous:swift,python` where the old script returned `swift`. Four regression assertions
  confirm single-domain swift/python/cpp-qt and empty-repo unknown are unchanged.
- **AC5 — stop is collision-only.** [test: DOMAIN stop is collision only] PASS.
- **AC6 — assertions added and suite passes.** [test: DOMAIN assertions present] PASS.
  13 assertions added to `scripts/tests/check_dev_jawn.sh`.
- **AC7 — reverting makes the assertions fail.** [manual] PASS, confirmed by the user on this evidence:
  - `git checkout HEAD~1 -- skills/submit/SKILL.md` → 5 named DOMAIN failures
  - `git checkout HEAD~1 -- scripts/detect_project_domain.sh` → 1 failure
  - both reverted → 6 failures
  - restored to HEAD → 0 failures

Reconciliation: 7 criteria in the issue, 7 observed, 7 passing.

## Caveats

- The fix is **not live in this session.** Skills load from the version-keyed plugin cache,
  and `~/.claude/plugins/cache/joec/dev-jawn/1.1.0/skills/submit/SKILL.md:137` still carries the
  old `unknown → STOP`. Deploying needs a version bump plus a Claude Code restart. This
  verification is against the repo working tree, which is where the acceptance criteria live.
- TDD is not provable from the log: the work landed as a single commit, so tests and
  implementation are indistinguishable in history. The assertions were written first and
  recorded 6 failures before the fix, but the commit history does not show that.
- No `[ui]` criteria, so the acceptance channel is the named tests rather than a UI harness.
