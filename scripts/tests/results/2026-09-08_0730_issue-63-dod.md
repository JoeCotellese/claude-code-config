# ABOUTME: Definition of Done results for issue #63 (/retro files upstream lessons as issues).
# ABOUTME: Records the channel-by-channel reconciliation of all nine acceptance criteria.

- **Date:** 2026-09-08
- **Issue:** #63 — Stop /retro writing across repositories: file upstream lessons as issues
- **Branch:** feature/63-retro-files-issues-upstream
- **Commit:** 6241a13
- **Status:** PASS
- **Domain:** `detect_project_domain.sh` returns `unknown`. Not a STOP here: the issue's
  Definition of Ready artifacts name the harness explicitly
  (`scripts/tests/check_issue_63.sh`) and record R7 as N/A under the `CLAUDE.md` markdown
  exemption. Nothing was guessed.

## Unit suite

`green`. Four shell suites, 119 assertions, zero failures, output pristine.

- `scripts/tests/check_dev_jawn.sh` — exit 0, 69 PASS, 0 FAIL
- `scripts/tests/check_issue_63.sh` — exit 0, 31 PASS, 0 FAIL
- `scripts/tests/check_issue_37.sh` — exit 0, 16 PASS, 0 FAIL
- `scripts/tests/check_hook_quiet.sh` — exit 0, 3 PASS, 0 FAIL

## Acceptance harness

`scripts/tests/check_issue_63.sh`, committed at `1bd6793` before any implementation and run
unmodified. Exit 0, all 31 assertions PASS.

## Criterion reconciliation — 9 of 9 observed, 9 of 9 passing

- **AC1** `[test: retro-no-hardcoded-config-path]` — PASS. Zero occurrences of
  `git/claude-code-config` in `retro/SKILL.md`; the Dependencies line no longer claims
  config-repo write access.
- **AC2** `[test: retro-does-not-branch]` — PASS. Neither `git checkout -b` nor `git commit`
  appears. The `:116` checkout went with the Step 5 rewrite.
- **AC3** `[test: retro-files-issues-upstream]` — PASS. Six assertions:
  `known_marketplaces.json`, `installed_plugins.json`, `gh issue create --repo`, and the
  `github` / `directory` / `remote get-url` resolution kinds, plus the
  never-edit-the-plugin-source rule.
- **AC4** `[test: retro-line-carries-scope]` — PASS. `status=proposed` and `amendment=` are
  gone; `scope=local`, `scope=upstream`, `scope=none` and `dest=` are documented.
- **AC5** `[test: skills-declare-max-words]` — PASS for all eight skills, each under both its
  declared ceiling and the gate cap: spec 2677/2950, ready 4001/4200, ui-design 1356/1500,
  implement 1565/1750, verify 1515/1700, submit 1954/2150, retro 1670/1900,
  create-goal 1343/1500.
- **AC6** `[test: retro-states-budget-rule]` — PASS. The ceiling paragraph names all three
  outcomes: replace a weaker counter-example, stay local only, consolidate first.
- **AC7** `[manual]` — **PASS.** Mr. Cotellese read Step 5 end to end on 2026-09-08 and
  answered that it reads as one decision procedure. Not self-certified.
  - Proxy A: `retro/SKILL.md` is 1670 words, under the 1900 gate cap. PASS.
  - Proxy B: exactly 3 distinct `scope=` values, no orphan fourth; the old
    `### Step 5 — Land the amendment in the right repository` heading is gone. PASS.
- **AC8** `[test: retro-flags-inert-local]` — PASS. Step 5's closing section states repo-local
  amendments are inert and names #64.
- **AC9** `[test: retro-handles-unresolved-upstream]` — PASS. The unresolvable branch prints
  the amendment and ends with `dest=unresolved`, with no fallback to editing.

## Durability beyond this issue

The `max_words` ceiling was folded into `scripts/tests/check_dev_jawn.sh` (the BUDGET block),
per the issue's note, so the invariant keeps running once #63 closes and its per-issue script
stops being interesting.

## Live verification of the resolution procedure

Ran Step 5's three steps by hand against this machine's real state:
`dev-jawn@joec` → marketplace `joec` → `directory` source at the local clone →
`git remote get-url origin` → `JoeCotellese/claude-code-config`. The `directory` kind, the one
dev-jawn actually ships under, resolves.

## Caveats

None. No workarounds, no retried steps, no screenshots (no UI surface in this issue).
